# Name of your application. Used to uniquely configure containers.
service: inventory

# Name of the container image. GHCR requires the owner namespace, so the full
# reference becomes ghcr.io/happy-parents/inventory.
image: happy-parents/inventory

# Deploy to these servers.
servers:
  web:
    - 193.169.188.144
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# TLS termination at kamal-proxy, paired with config.assume_ssl and
# config.force_ssl in production.rb.
#
# Let's Encrypt (`ssl: true`) is deliberately NOT used here. Kamal's automatic
# certificates are validated over port 443, and this hostname is proxied by
# Cloudflare, which terminates TLS at its own edge and never forwards the
# challenge to this origin. So the certificate is supplied explicitly below and
# Cloudflare re-encrypts to us.
#
# Cloudflare's SSL/TLS mode must be "Full", or "Full (Strict)" once the
# certificate here is issued by the Cloudflare Origin CA rather than self-signed.
proxy:
  host: inventory.happy-parents.ua

  # Secret names, not values; resolved from .kamal/secrets.
  ssl:
    certificate_pem: TLS_CERTIFICATE_PEM
    private_key_pem: TLS_PRIVATE_KEY_PEM

  # kamal-proxy drops X-Forwarded-* once ssl is set, but Cloudflare sits in
  # front of it as a trusted proxy, so keep the client IP and scheme it sends.
  forward_headers: true

# Where you keep your container images.
registry:
  server: ghcr.io

  # Both are secret references resolved from .kamal/secrets, which reads them
  # out of the encrypted production credentials. The password must be a PAT
  # with write:packages, never an account password.
  username:
    - KAMAL_REGISTRY_USERNAME
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - DB_PASSWORD
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Points at the Postgres accessory below, reachable on the local Kamal
    # docker network by its accessory hostname.
    DB_HOST: inventory-db
    DB_USERNAME: hp_inventory

    # Puma threads and therefore the Active Record pool size. Solid Queue runs
    # in-process here (SOLID_QUEUE_IN_PUMA above) and needs its 3 worker
    # threads plus 2 connections, so anything below 5 makes it warn and throttle.
    RAILS_MAX_THREADS: 5

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Persistent volume for local Active Storage uploads. The databases now live in
# the Postgres accessory below, which keeps its own `data` directory.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "inventory_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.4.8
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Deploy as the unprivileged `deployer` user (uid 1000, member of the docker
# group, no sudo) rather than root. The key is named explicitly so deploys do
# not depend on whatever ~/.ssh/config or the agent happens to offer.
ssh:
  user: deployer
  keys:
    - ~/.ssh/koi_vps_deployer

# Use accessory services (secrets come from .kamal/secrets).
accessories:
  db:
    image: postgres:17
    host: 193.169.188.144
    # Bind to localhost only; the app reaches it over the Kamal docker network.
    port: "127.0.0.1:5432:5432"
    env:
      clear:
        POSTGRES_USER: hp_inventory
        POSTGRES_DB: hp_inventory_production
      secret:
        - POSTGRES_PASSWORD
    directories:
      - data:/var/lib/postgresql/data
